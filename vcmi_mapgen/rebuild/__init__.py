"""The identity-rebuild engine: shape-relative zone templates, bit-exact same-shape replay,
and warp adaptation onto a different shape. ``cli.py`` builds a ``Pipeline`` (see
``pipeline.py``) directly on top of this; import submodules directly, e.g. ``from
vcmi_mapgen.rebuild import engine``.
"""
